---
description: Design one approved feature for React, FastAPI, PostgreSQL, and the platform compatibility boundary.
agent: migration-coordinator
---

Design feature: $ARGUMENTS

Confirm a behavior contract exists and is sufficiently approved. Delegate to `migration-designer` using `target-feature-design`. Do not mechanically preserve WPF/C#/MSSQL structure. Record provisional decisions if the DLL host contract is still unresolved.
