---
description: Build or refine an evidence-graded behavior contract for one discovered feature.
agent: migration-coordinator
---

Specify feature: $ARGUMENTS

Use `behavior-contract`, `evidence-grading`, and `uncertainty-management`. Build the contract from discovered evidence, not desired target architecture. Persist unresolved semantics and stop at the human gate when a material unknown blocks design.
