---
description: Verify one migrated feature against available legacy evidence and observable behavior.
agent: migration-coordinator
---

Verify feature: $ARGUMENTS

Delegate to `verifier` using `parity-verification`. Use the strongest available composite judge and produce PASS/FAIL/PARTIAL/BLOCKED with explicit unverified behavior. Update queue/state based on the result.
